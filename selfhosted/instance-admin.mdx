---
title: "Instance administration"
description: "Understand instance-wide administrator responsibilities on a self-hosted SlugBase deployment."
---

## What you'll learn

After reading this guide, you will know what an instance administrator can do, how the role differs from workspace admin, and how to run a self-hosted deployment without a separate operator console.

## Instance administrator role

An **instance-wide administrator** is a user account flagged to govern the entire deployment. This role is distinct from **workspace admin**:

| Scope | Instance administrator | Workspace admin |
|-------|---------------------|-----------------|
| Create workspaces | Yes | No (unless also instance admin) |
| Invite users into workspaces | Via workspace admin flows | Yes, within their workspace |
| Configure SMTP / OIDC / AI in UI | Yes, when admin UI is enabled | Yes, for their workspace |
| Reset user MFA | Yes (self-hosted recovery path) | No |
| Manage billing | N/A on self-hosted | N/A on self-hosted |

There is **no separate operator console in v1**. Instance administrators use the same SlugBase web application as everyone else, plus deployment-level configuration through environment variables and infrastructure tools.

<Callout kind="info" title="Screenshot placeholder">
**Suggested capture:** First-run setup screen creating the initial admin account and first workspace.

**Suggested filename:** `/assets/selfhosted/setup-admin-account.png`

**Alt text:** Initial setup form for admin account and first workspace name.
</Callout>

## First-run setup

When the database has no users, visiting the instance opens a one-time **Set up your instance** flow:

<Steps>
  <Step title="Create the admin account">
    Enter your name, email, and password. This account becomes the instance administrator.
  </Step>
  <Step title="Create the first workspace">
    Name your first workspace and finish setup.
  </Step>
</Steps>

After setup, the setup flow is no longer available.

## Ongoing administration tasks

### Create workspaces and onboard users

Self-hosted SlugBase is an **admin-curated multi-workspace** product:

1. Create workspaces from the **New workspace** action in the workspace switcher.
2. Invite members from **Settings → Administration → Members & teams**.
3. Keep **public registration** disabled unless you intentionally want open signup.

### Configure email and identity

When the admin UI is enabled (`VITE_MAIL_ADMIN_UI=true`, `VITE_OIDC_ADMIN_UI=true`), configure:

- [SMTP](/selfhosted/smtp) for outbound email
- [OIDC](/selfhosted/oidc) for federated sign-in

Alternatively, set transport and identity via deployment environment variables and disable the admin UI panels.

### Promote additional instance administrators

Instance admin promotion is performed at the deployment level (database flag on the user account). The first account created at setup is an instance administrator by default.

### Recover locked-out users

If a user loses access to MFA and backup codes, an instance administrator can reset MFA directly — the self-hosted recovery path documented in the product spec.

## Deployment configuration

Operators also manage infrastructure settings outside the web UI:

| Area | Examples |
|------|----------|
| URLs | `APP_BASE_URL`, `FRONTEND_ORIGIN` |
| Secrets | `SESSION_SECRET`, `ENCRYPTION_KEY` |
| Database | `DATABASE_URL` |
| Registration | `PUBLIC_REGISTRATION` (default `false` on self-hosted) |
| Admin UI visibility | `VITE_MAIL_ADMIN_UI`, `VITE_OIDC_ADMIN_UI` |

See your operator runbook and [Quick start](/selfhosted/quick-start) for install and upgrade procedures.

## What instance admins do not have in v1

- A dedicated super-admin dashboard or account directory UI
- First-class backup/restore beyond [export](/selfhosted/backup-and-export) and database volume copy
- Hosted-style billing or plan management

## Related topics

- [Quick start](/selfhosted/quick-start) — install and first run
- [Workspaces overview](/selfhosted/workspaces-overview) — multi-workspace model
- [Members and teams](/selfhosted/members-and-teams) — invitations
- [Troubleshooting](/selfhosted/troubleshooting) — common operator issues
