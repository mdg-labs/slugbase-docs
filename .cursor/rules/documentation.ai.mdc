---
description: Documentation.AI authoring for customer/operator MDX in this repo. Frontmatter, navigation, CDN images, and components. Parent epic #392.
globs:
  - "**/*.mdx"
  - "documentation.json"
alwaysApply: false
---

# Documentation.AI — SlugBase public docs

You are writing **customer and operator** documentation published via [Documentation.AI](https://documentation.ai). Source lives in **this repository** ([`mdg-labs/slugbase-docs`](https://github.com/mdg-labs/slugbase-docs)) at the repo root. Push to `main` to publish at [docs.slugbase.app](https://docs.slugbase.app). Engineering specs stay in the sibling **`slugbase`** monorepo (`slugbase/docs/internal/`) and are **never** part of customer docs.

**Authoring guide:** `README.md` · **Workflow skill:** `slugbase/.cursor/skills/write-customer-docs/SKILL.md` (monorepo) · **Parent contract:** GitHub #392

**Local setup:** open [`slugbase/slugbase.code-workspace`](../slugbase/slugbase.code-workspace) for both repos (`slugbase/` + `slugbase-docs/`).

## Product vocabulary (spec §3 — mandatory in public docs)

| Use | Never use |
|-----|-----------|
| workspace | organization, org, tenant |
| folder | collection |
| pinned / pinning | favorite, starred |
| member | user (for workspace membership) |
| slug | alias, shortlink, keyword |
| forwarding / go | redirect (in user-facing copy) |

If a reader needs to be a SlugBase **developer** to understand a page, it belongs in `slugbase/docs/internal/`, not here.

## Page files (MDX)

Every page **must** start with YAML frontmatter:

```yaml
---
title: "Clear, specific, keyword-rich title"
description: "Concise description explaining page purpose and value"
---
```

- H1 comes from frontmatter `title` — start body content with `##` (H2)
- Filenames: **lowercase-with-hyphens** (`quick-start.mdx`, not `QuickStart.mdx`)
- Internal links: **root-absolute** — `[Quick start](/ce/quick-start)`, not relative `../`
- Never commit secrets, API keys, or real credentials in examples
- **Escape angle-bracket placeholders** — MDX treats `<slug>`, `<token>`, and similar as JSX tags. Write `/go/&lt;slug&gt;`, `go &lt;slug&gt;`, and `Bearer &lt;token&gt;`. Never use `{/* screenshot:… */}` comments; use `<Callout kind="info" title="Screenshot placeholder">` instead
- **Every `<Callout>` needs a `title`** attribute
- **Escape `&` in JSX attributes** — e.g. `title="Members &amp; teams"`

## documentation.json (navigation)

Lives at `documentation.json` (repo root). Controls branding, navbar, sidebar, and OpenAPI wiring.

**Adding a page requires both:**

1. Create `{product}/{page}.mdx` with frontmatter (e.g. `ce/quick-start.mdx`)
2. Add a `path` entry in `documentation.json` under the correct `navigation.products → tabs → groups → pages` chain

| `documentation.json` | File on disk |
|----------------------|--------------|
| `"path": "ce/quick-start"` | `ce/quick-start.mdx` |
| `"path": "cloud/billing"` | `cloud/billing.mdx` |

- `path` is relative to repo root, **no** `.mdx` suffix
- `title` in JSON is sidebar label (can differ from frontmatter `title`)
- `icon` optional — Lucide name (e.g. `"zap"`, `"book-open"`)
- `href` instead of `path` for **external** nav page links only
- Navbar action links use **`link`**, not `href` — an invalid key crashes the entire site at prerender with no build warning:

```json
"navbar": {
  "actions": {
    "links": [
      { "title": "GitHub", "link": "https://github.com/mdg-labs/slugbase" }
    ]
  }
}
```

- Each nav container uses **one** child type: `groups` OR `pages` OR `dropdowns`, not a mix

## Custom CSS (`styles/slugbase.css`)

SlugBase branding beyond `documentation.json` `colors` is in `styles/slugbase.css`, registered via:

```json
"css": [{ "src": "styles/slugbase.css" }]
```

Rules:

- Paths are **relative to repo root** — no leading `/`, `./`, or `../`
- File must exist before referencing; max 5 MB; `.css` only
- Styles load **after** built-in theme — override DA CSS variables (`--brand`, `--navbar-bg`, `--sidebar-bg`, …) and stable `dai-*` selectors (see [Custom CSS](https://documentation.ai/docs/customize/custom-css))
- Dark mode: scope overrides under `.dark`
- Publish required for live site changes (push to `main` or Editor **Publish**)

**OpenAPI API tabs:** specs live beside product guides:

- `ce/api-reference/ce-openapi.yaml`
- `cloud/api-reference/cloud-openapi.yaml`

Reference from a nav **group** via `"openapi": "…"`. Documentation.AI generates API pages from the spec.

---

## Core writing principles

### Audience and tone

- Write for **customer and operator** audiences — people using SlugBase or running a deployment, not engineers building the product (spec §2.4)
- **Task-first:** open with what the reader needs to accomplish; explain concepts only when needed to complete the task
- Clear, direct language; second person ("you"); active voice; present tense for current states
- Define jargon on first use; consistent terminology (see [Product vocabulary](#product-vocabulary-spec-3--mandatory-in-public-docs))
- Concise sentences; parallel structure in lists and headings

### Content organization

- Lead with the task or outcome, then prerequisites and context
- Progressive disclosure: basics before advanced topics
- Numbered steps for procedures; prerequisites before instructions
- Descriptive, keyword-rich headings (H2 → H3 → H4)
- Verification steps so readers can confirm success

### User-centered approach

- Focus on user goals and outcomes, not internal implementation
- Anticipate common questions; include troubleshooting for likely failures
- Write for scannability: headings, lists, callouts, white space

## Screenshots and images

Documentation.AI does **not** serve arbitrary repo files at `/assets/…` on the live site. Use the **web editor** to upload screenshots — images are optimized and published on `blob-cdn.documentation.ai`.

**Preferred workflow:**

1. Upload via Documentation.AI web editor (`/image` command).
2. Copy the CDN URL from image properties.
3. Reference in MDX with `<Image>`:

```mdx
<Image
  src="https://blob-cdn.documentation.ai/org-…/screenshot.png?q=85&fm=auto&auto=compress%2Cformat"
  alt="Descriptive alt text for the UI shown"
  width="1200"
  height="800"
/>
```

Until a screenshot exists, use a placeholder callout:

```mdx
<Callout kind="info" title="Screenshot placeholder">
**Suggested capture:** …
**Alt text:** …
</Callout>
```

Do **not** use `![alt](/assets/…)` — those paths 404 on the published site.

## Documentation.AI components

**Attribute naming:** kebab-case (`param-type`, `default-open`, `show-lines`). Boolean attributes: `required="true"` or `horizontal={true}`.

**Icons:** Lucide names without suffix — `icon="zap"`, `icon="book-open"` ([Lucide icons](https://lucide.dev/icons/)).

### Callouts

Use `<Callout kind="info|tip|alert|danger|success">` for supplementary, best-practice, caution, high-risk, and confirmation content.

### Code

- Fenced blocks with language tag; optional `title="filename.ext"`
- `<CodeGroup>` for multi-language equivalents
- `<Request>` / `<Response>` for API endpoint sidebar examples
- Complete, runnable examples with realistic (non-secret) data

### Structure

- `<Steps>` for sequential procedures (prefer over long ordered lists)
- `<Tabs>` for platform-specific or alternative approaches
- `<Expandable>` / `<ExpandableGroup>` for progressive disclosure
- `<Card title="…" href="…">` inside `<Columns>` for navigation grids
- `<ParamField>` / `<ResponseField>` for API parameters and responses
- `<Update>` for changelogs and release notes
- Mermaid for flowcharts and sequence diagrams

### Component selection

| Need | Use |
|------|-----|
| Page structure | Markdown H2–H4 |
| Procedures | `<Steps>` |
| Platform variants | `<Tabs>` |
| Multi-language code | `<CodeGroup>` |
| API params/responses | `<ParamField>`, `<ResponseField>`, `<Request>`, `<Response>` |
| Highlights | `<Callout>` |
| Navigation grids | `<Card>` in `<Columns>` |
| Media | `<Image>`, `<Video>`, `<Iframe>` |

## References

- `slugbase/.cursor/skills/write-customer-docs/SKILL.md` — greenfield and maintenance authoring workflows (`/write-customer-docs`)
- [Documentation.AI code editor](https://documentation.ai/docs/write-and-publish/code-editor)
- [Documentation.AI images](https://documentation.ai/docs/components/images)
- [Site configuration](https://documentation.ai/docs/customize/site-configuration)
- [documentation.json schema](https://dashboard.documentation.ai/documentation.json)
- SlugBase publish verification: `slugbase/docs/internal/documentation-ai-publish-verification.md`
